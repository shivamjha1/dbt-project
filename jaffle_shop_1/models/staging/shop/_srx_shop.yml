sources:
  - name: shop
    database: raw
    schema: shop
    tables:
      - name: customers
        columns:
          - name: id
            description: primary key
            data_tests:
              - not_null
              - unique
      - name: orders
        config:
          freshness:
            warn_after: 
              count: 24
              period: hour
            error_after:
              count: 96
              period: hour
          loaded_at_field: _etl_loaded_at
