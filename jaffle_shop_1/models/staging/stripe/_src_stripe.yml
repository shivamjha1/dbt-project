sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payments
        config:
          freshness:
            warn_after: 
              count: 24
              period: hour
            error_after:
              count: 96
              period: hour
          loaded_at_field: _etl_loaded_at